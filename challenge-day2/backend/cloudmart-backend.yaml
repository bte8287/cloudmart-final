apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudmart-backend-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cloudmart-backend-app
  template:
    metadata:
      labels:
        app: cloudmart-backend-app
    spec:
      serviceAccountName: cloudmart-pod-execution-role
      containers:
      - name: cloudmart-backend-app
        image: public.ecr.aws/s1z5c5n6/cloudmart-backend:latest
        imagePullPolicy: Always
        env:
        - name: PORT
          value: "5000"
        - name: AWS_REGION
          value: "us-east-1"
        - name: BEDROCK_AGENT_ID
          value: "XXXXX"
        - name: BEDROCK_AGENT_ALIAS_ID
          value: "XXXXX"
        - name: OPENAI_API_KEY
          value: "XXXXX"
        - name: OPENAI_ASSISTANT_ID
          value: "XXXXX"
        - name: AZURE_ENDPOINT
          value: "XXXXX"
        - name: AZURE_API_KEY
          value: "XXXXX"
---

apiVersion: v1
kind: Service
metadata:
  name: cloudmart-backend-app-service
spec:
  type: LoadBalancer
  selector:
    app: cloudmart-backend-app
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
